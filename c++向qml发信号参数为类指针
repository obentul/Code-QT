当从c++里向qml发信号时，如果传的是指针，那么可以理解为qml里面拿的也是指针(用法不是->，用.)，这就有一个现象，那就是在qml中修改传出的这个对象时，
是以指针模式修改的。 

（！）所以qml中的改动会影响c++中emit出去的指针指向的那个对象的值。如果在emit出去后，又建立了依赖关系，那么由于指针的值变了，依赖关系也会被触发。


小结：如果从c++向qml emit指针值，那么qml中拿到的也是指针，那么对指针值的修改将遵循指针的特性，即会影响指针指向那个对象的值。



PS：如果emit出去的不是指针，仅仅是一个值，那么特性是怎么样的呢？？？
待完善
