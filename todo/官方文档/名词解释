1.信号处理函数：	对应于c++中的槽(slot)，就是linux中的信号处理函数，只不过c++把这个东西单独弄出来给起了个名字，应该是为了和普通函数在字面上区分开，让人一看到就知道
 (Signal Handler)	这个函数时信号处理函数，而不是普通函数。

2.组件			是一个QML对象或者对象树(!!!)创建时，所依赖的模板。当使用QML engine加载某个组件的时候，它便可以实例化了。
 (Component)		组件类型可以通过使用Qt.createComponent()来动态创建一个QML对象

3.对象			实例
 (Object)		

4.Qt Quick		支撑QML的c++库
